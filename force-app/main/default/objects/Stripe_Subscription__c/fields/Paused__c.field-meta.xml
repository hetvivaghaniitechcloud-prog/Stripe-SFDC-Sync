<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Paused__c</fullName>
    <defaultValue>false</defaultValue>
    <description>True when Stripe pause_collection is set (payment collection paused). Stripe keeps status=active while paused, so this flag distinguishes Active vs Paused for the portal UI.</description>
    <label>Paused</label>
    <trackTrending>false</trackTrending>
    <type>Checkbox</type>
</CustomField>
