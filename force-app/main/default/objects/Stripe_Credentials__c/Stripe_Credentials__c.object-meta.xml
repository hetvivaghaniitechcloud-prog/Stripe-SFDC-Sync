<?xml version="1.0" encoding="UTF-8"?>
<CustomObject xmlns="http://soap.sforce.com/2006/04/metadata">
    <customSettingsType>Hierarchy</customSettingsType>
    <description>Stores Stripe connector settings: environment, publishable keys, named-credential names, webhook signing secrets (rotation-aware), enabled checkout modes, URLs and notification toggles. Protected for AppExchange. The Stripe SECRET key is NEVER stored here - it lives only in the Named Credential.</description>
    <enableFeeds>false</enableFeeds>
    <label>Stripe Credentials</label>
    <visibility>Protected</visibility>
</CustomObject>
